"""Shared helper components for audio models.

Convention: this package's ``__init__`` performs no re-exports (consistent
with the BEATs/CLAP precedent); callers import directly from the specific
submodule (e.g. ``timbral.models.helpers.panns``); the same applies to the
registry.
"""
